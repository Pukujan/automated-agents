# Current continuation point

## State

The content-first baseline has been superseded by docs/PLAN.md and the research-first milestone. Research, brand, and strategy own separate proposed artifacts. Local browser exploration and incremental collection are the intended first operational slice.

This repository contains design documents and instructions, not running agents or browser automation. No live crawl, analytics baseline, or campaign has been performed.

## Next task

Use issue #1. Inspect the local browser interface, choose one explicit question and seed set, and run a bounded visible collection. Verify saved observations before deriving reusable navigation/extraction routines. Keep the first output useful: evidence plus findings and a provisional strategy.

## Durability

Public design lives in Git. Private hypotheses/evidence belong in FOSSIL, not this repository. Local save receipts must state whether they have been replicated or backed up; none is implied by this document. See docs/storage.md.

## Open choices

Browser bridge, actual runtime, first research question/seeds, profile inputs, unattended execution, and backup destination. Default budgets in PLAN.md are proposals, not approved platform limits.

## Run handoff template (keep real entries private)

- Task/question and owner:
- Code, module, profile and proposal revisions:
- Input/output evidence references:
- Actual page/time/expansion budget and coverage:
- State: ready / running / paused / partial / complete / blocked:
- Findings versus assumptions:
- Proposed next action:
- User decision, if any:
- Persistence receipt and replication status:
